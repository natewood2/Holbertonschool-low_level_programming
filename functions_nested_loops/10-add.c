#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 * Description: adds two integers and returns a result.
 * @o: first number input
 * @s: second number input
 * Return: one + two
 */

int add(int o, int s)

{
	return (o + s);
}
